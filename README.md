# Installation

```
1. create .env file containing API_HOST with your server as the value (example: http://localhost:8080)
2. npm install
3. npm run tailwind (only first time)
4. npm run dev-css (in terminal 1)
5. npm run dev (in terminal 2)
```
